# Production dependencies for Vercel deployment
# Minimal set to reduce bundle size

# Web server (essential)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0

# Core game (essential)
bitarray>=3.8.1

# Performance - Numba JIT (try to fit under 250MB)
numba>=0.59.0
numpy>=1.26.0
