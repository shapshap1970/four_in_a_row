# Core dependencies (for gameplay)
# None required! Uses only Python standard library

# Optional: For building Windows executable
pyinstaller>=6.11.1

# Legacy dependencies (not used by main game) - UPDATED FOR SECURITY
rich>=15.0.0
bitarray>=3.8.1

# Security scanning (recommended for development)
bandit>=1.7.10
pip-audit>=2.7.0