# Production dependencies for Vercel deployment
# Minimal set to reduce bundle size

# Web server (essential) - NO [standard] to save ~20MB
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.10.0

# Core game (essential)
bitarray>=3.8.1

# Rust AI extension - TODO: Add when wheel is available
# For now, falls back to Python AI at depth 9
# four-in-a-row-rust>=0.1.0
