# Production dependencies for Vercel deployment
# Minimal set to reduce bundle size

# Web server (essential) - NO [standard] to save ~20MB
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.10.0

# Core game (essential)
bitarray>=3.8.1

# Rust AI extension (~185KB, depth 12!)
./wheels/four_in_a_row_rust-0.1.0-cp311-cp311-manylinux_2_17_x86_64.manylinux2014_x86_64.whl
